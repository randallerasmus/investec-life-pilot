# /lifepilot

Load `LIFEPILOT_CONTEXT.md` from the repository root and use it as the active project context.

After loading it, summarize only the current LifePilot project state and ask what the user wants to work on next unless the user already gave a specific task.

