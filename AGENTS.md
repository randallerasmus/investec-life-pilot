# Agent Context

This repository is the LifePilot MVP.

Before making changes or answering project-specific questions, read `LIFEPILOT_CONTEXT.md`. It contains the project purpose, implemented endpoints, current limitations, development commands, and Investec readiness assessment.

When the user says `/lifepilot`, load `LIFEPILOT_CONTEXT.md` and use it as the working brief for the session.

When the user says `/moneycoach`, treat it as a compatibility alias for `/lifepilot`. Money Coach is now the internal budgeting module inside LifePilot.
